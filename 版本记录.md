# 版本记录（每一版的 主要功能 / 用法 / 用户评价）

> 约定：本仓库从「0.3m 热力密度图」这一版开始记录版本。每新增一版提交，就追加一条。
> 「用户评价」记录用户对该版的反馈、要求或修改意见（据此决定后续开发方向）。

---

## v0.1 ｜ 首个入库版本：0.3m 热力密度图版（当前）
- **日期**：2026-08-30
- **主要功能**：
  - 读取地下车库点云（`.las` 与 `.ply`，兼容带/不带 RGB）。
  - `0.5m` 体素降采样 → 去孤立噪点 → 只保留最大连通聚合体 → 法向量判别去地面/天花板 → 投影到水平面（Z 归零）。
  - 输出：外墙点云 `_wall.ply`、`_俯视_原始/外墙.png`、`_透视_外墙.png`、`汇总.json`。
  - **新增 `0.3m × 0.3m` 热力密度图** `_热力密度图.png`：用**原始分辨率点**（仅限墙体结构内）统计密度，
    采用对数色标（`LogNorm`）渲染，突出「外墙密集连续」的墙带。
- **用法**：
  ```bash
  python code/preprocess.py CLEAN_UNDER_GROUND.ply RESULT_B1.ply
  ```
- **用户评价/反馈（本期汇总）**：
  - 最初要求仅处理 LAS，必须做 0.5m 降采样，且要有可视化图片供查看。
  - 之后改为换两个 PLY（CLEAN_UNDER_GROUND、RESULT_B1）运行，并删除之前 LAS 的结果。
  - 下一步要求做 **0.3m 热力密度图** —— 本版已实现。
  - 同时要求建立 GitHub 仓库、把每一版代码入库并标注（功能 / 用法 / 评价），且 `.gitignore` 不上传。
  - 处理结果：粟塘体约 256×93 m、雅德体约 75.7×54.7 m，外墙墙带在密度图上明显高亮。

---

## v0.2 ｜ 规则外轮廓提取版（`code/contour.py`）
- **日期**：2026-08-30
- **主要功能**：
  - 从阶段一输出的 `*_wall.ply`（投影后 Z=0 的外墙点云）提取**规则外轮廓多边形**。
  - 方法：凹包（`shapely.concave_hull`，`CONCAVE_RATIO=0.15`）→ 取最大多边形 → Douglas-Peucker 简化（`SIMPLIFY_TOL=0.8`，可并存）成直线段多边形。
  - 同时输出**最小外接旋转矩形**作为参考（绿色虚线，比真实轮廓更粗放）。
  - 输出：`*_外轮廓.png`（墙点 + 外轮廓 + 外接框）、`*_外轮廓.json`（顶点坐标、面积、周长、外接框）、`外轮廓_汇总.json`。
  - 参数在 `code/contour.py` 顶部可调：`CONCAVE_RATIO`（越小越贴凹边/1=凸包）、`SIMPLIFY_TOL`（简化容差）。调小可更贴合真实墙角但顶点更多。
- **用法**：
  ```bash
  python code/contour.py 输出/CLEAN_UNDER_GROUND_wall.ply 输出/RESULT_B1_wall.ply
  ```
- **用户评价/反馈（本期）**：
  - 要求：进行下一步优化（规则外轮廓提取），上传代码外还需告知**输出结果位置**，用户会据此让步骤调试调整。
  - 效果：CLEAN 31 顶点（面积 12142 m²/周长 681 m，贴合右下延伸块与顶部缺口）；RESULT 10 顶点（面积 2449 m²，贴合旋转矩形底部缺口）。
  - 待优化方向（用户可能随后要求）：顶点偏多/偏毛糙、是否需要把边吸附到主轴、是否只保留外墙壳（去掉内部柱）、简化容差/凹包比率的取舍。

---

## v0.3 ｜ 贴合外墙轮廓线版（`code/contour.py`，方法重写）
- **日期**：2026-08-30
- **主要功能**：改为输出**贴合外沿墙体走向的轮廓线**，而不是用凹包把区域包成闭合多边形。
  - 方法：墙点栅格化(0.5m) → 闭运算连缝 → `binary_fill_holes` 填内部空腔 → `binary_opening` 去一格外伸细刺 → 提取外沿轮廓（marching squares）→ shapely 简化。
  - 差异：这条线**沿真实墙体外沿走线**，凹口/空腔处不斜穿、不包住（比凹包面积更小、周长更长、顶点更多）。
  - 参数：`RASTER_GRID=0.5`(栅格)、`CLOSE_ITER=2`(连缝)、`OPEN_KERNEL=2`(去刺)、`SIMPLIFY_TOL=0.8`。输出 `*_外轮廓.png`、`*_外轮廓.json`、`外轮廓_汇总.json`。
- **用法**：同 v0.2（`python code/contour.py 输出/*_wall.ply`）。
- **用户评价/反馈**：用户明确指出「不要凹包那种把区域包起来的轮廓，要一条**贴合外部轮廓的线**」——据此将方法从 concave hull 改为栅格+外沿提取。效果：CLEAN 108 顶点（面积 9630 m²/周长 905 m）、RESULT 28 顶点（面积 2095 m²/周长 252 m），均贴合真实墙沿与转角。
- **待优化方向**（用户可能随后要求）：贴得够但仍有 0.5m 栅格阶梯锯齿；是否把 `SIMPLIFY_TOL` 调大更平滑、或把长边吸附到主轴成更"规则"折线；右侧凹口是否要保留（可能为扫描缺失）。

---

## v0.4 ｜ 规则化外轮廓版（`code/contour.py` 增加规则化）
- **日期**：2026-08-30
- **主要功能**：在 v0.3 贴合线基础上新增**规则化**，输出"工程图样"式规整多边形：
  - 取主方向（最小外接矩形）→ 旋转转正 → Douglas-Peucker 简化（`REG_TOL`，把阶梯去成一条直边，含统一斜角，如雅德的旋转矩形）→ 转回原方向。
  - 参数：`REG_TOL=4.0`（越大越规则、顶点越少）。输出同名 `*_外轮廓.png` / `.json`。
- **用法**：同 v0.2/v0.3（`python code/contour.py 输出/*_wall.ply`）。
- **用户评价/反馈**：用户指出「大体贴合，但真正工程图样不长这样，要做成规则的形状」——据此加规则化。
  - 效果：CLEAN 35 顶点（9747 m²/817 m；v0.3 为 108 顶点）、RESULT 13 顶点（2132 m²/240 m），RESULT 已是干净的旋转矩形。
- **待优化方向**：若还想更平滑、或去掉某处凹口（如 CLEAN 底部 V 形连接凹口）、或吸附到严格横平竖直，可再调 `REG_TOL`，或我再加"方向吸附/直交化"。

---

## v0.5 ｜ 修复轮廓坐标偏移 + 加强可视化（bugfix）
- **日期**：2026-08-30
- **主要功能 / 修复**：
  - **修复坐标整体平移**：v0.4 的"绕原点转正再转回"因车库坐标离原点几百米、旋转角较大，使多边形整体偏移几百米，导致轮廓跑到点云视野之外、用户"看不到"。已改为直接 Douglas-Peucker（与方向无关），坐标归位。
  - 可视化增强：轮廓画成**粗红实线 + 顶点圆点**；新增**纯轮廓线图 `*_轮廓线.png`**（白底、红粗线、含顶点），便于一眼查看。
- **用法**：同前（`python code/contour.py 输出/*_wall.ply`）。
- **用户评价/反馈**：用户「轮廓是什么颜色，我看不到你提取出来的轮廓」——排查发现是坐标被旋转平移所致，修复后轮廓以**红色实线**清晰可见。RESULT 13 顶点、CLEAN 35 顶点。

---

## v0.6 ｜ 外圈高密度点云高亮版（`code/outer.py` 新增）
- **日期**：2026-08-31
- **主要功能**：**不描轮廓线**，把**外侧高密度点云（外墙）用红色**高亮，其余点浅色。
  - 方法：墙点栅格化(0.5m) → 闭运算连缝 → 填内部空腔 → `distance_transform_edt` 求"到外边界距离" → 距离 ≤ `BAND` 判为外圈点 → 标红。
  - 输出：`*_外圈点云.png`（红=外墙外圈，蓝=内部/其余点）。参数 `GRID=0.5`、`CLOSE_ITER=2`、`BAND=2.0`(m)。
- **用法**：`python code/outer.py 输出/*_wall.ply`。
- **用户评价/反馈**：用户「先不要描外轮廓，先把外面高密度的点云用红色表示出来」——据此新增。效果：外墙在图中成明显红色外环，内柱浅色。外圈点占比：CLEAN 43%(15676/36474)、RESULT 52%(5041/9753)。
- **待调**：`BAND` 控制外圈厚度（如需更细/更粗的外圈环即调它）。

---

## v0.7 ｜ 原始点竖直密度 + 外圈位置（`code/outer.py` 重写）
- **日期**：2026-08-31
- **主要功能**：把"外面高密度点云"改为**两者结合**定位：
  - **竖直堆叠密度**：同一 0.3m XY 小柱内的点数（`COL_GRID=0.3`）。墙体/柱竖直叠满→密度高；地面/天花板只顶底各一层→密度低。取**前5%**（`PCT=95`）为高密度。
  - **外圈位置**：距外边界 ≤ `BAND`(1.5m)（`GRID=0.5` 栅格 + `binary_fill_holes` + 距离变换）。
  - `高密度 AND 在外圈` → 红（外圈高密度墙）；`高密度但不在外圈` → 橙（内部密集柱，被位置条件剔除）；其余浅蓝。
  - 输入**原始** `.ply`；输出 `*_外圈点云.png`。
- **用法**：`python code/outer.py ../CLEAN_UNDER_GROUND.ply ../RESULT_B1.ply`。
- **关键说明**：不能用原始点"平面(XY)密度"——地面/天花板平面铺满、XY 最密，会误判为墙；需用**竖直堆叠密度**才贴合"外墙垂直方向密集连续"的物理特征。
- **用户评价/反馈**：用户确认"密度前5%判外墙"并选"用原始点算密度"；指出此前外圈太粗。结果（含位置约束）：RESULT 红点 6022(2.9%)、CLEAN 红点 6607(6.0%)，外墙成**完整细红环**，内部柱为橙色（被剔除）。
- **参数**：`COL_GRID=0.3`、`GRID=0.5`、`CLOSE_ITER=2`、`PCT=95`、`BAND=1.5`。

---

## v0.8 ｜ 修复连续性问题（外圈红环改为"细外圈带"驱动）
- **日期**：2026-08-31
- **主要功能 / 修复**：v0.7 用"前5%硬阈值"切外墙，部分墙体密度略低被切掉造成**断口**。改为：
  - 红环 = **距外边界 ≤ `BAND`(0.8m) 的细外圈带**，来自填充 footprint 的边界 → **天然连续**；
  - 内部密集柱因**远离边界**被位置约束自动剔除，密度（前5%）仅作为独立标识（橙色），不再用于硬切外墙造成断口。
  - 兼顾"连续"与"细"：比 v0.6 细，比 v0.7 连续。
- **用法**：`python code/outer.py ../CLEAN_UNDER_GROUND.ply ../RESULT_B1.ply`。
- **用户评价/反馈**：用户「现在反而没有之前连续了」——据此把红环从"密度与"改回"细外圈带"驱动。结果：RESULT 红点 24818(11.9%)、CLEAN 红点 12639(11.4%)，外墙连续细环，内部柱橙色。
- **参数**：`BAND=0.8`（控制红环粗细）、`COL_GRID=0.3`、`GRID=0.5`、`PCT=95`。

---

## v0.9 ｜ 双重密度门槛（去误差点云）
- **日期**：2026-08-31
- **主要功能**：标记条件收紧为**双重密度门槛 + 外圈位置**，用于剔除内部"误差点云"：
  - **全局前5%**：竖直堆叠密度 ≥ 全点云密度的95分位（`GLOBAL_PCT=95`）；
  - **局域前10%**：密度 ≥ 该点所在 12m 局部区域内密度的90分位（`LOCAL_PCT=90`、`LOCAL_CELL=12`）；
  - 两者**同时满足**（AND）→ 双重高密度；再 **AND 外圈位置**（距外边界 ≤ `BAND=1.0m`）→ 红（外墙）。
  - 双重高密度但不在外圈 → 橙（内部密集柱）。
- **用法**：`python code/outer.py ../CLEAN_UNDER_GROUND.ply ../RESULT_B1.ply`。
- **用户评价/反馈**：用户指出"里面的误差点云去不掉，所以不仅要局域前10%，还必须全局前5%才可以标记"——据此把密度门槛改为双条件。
  - 结果：RESULT 红点 4838(2.3%)、CLEAN 红点 5415(4.9%)，误差/错检点被大幅剔除，外墙环连续细。
- **参数**：`COL_GRID=0.3`、`GRID=0.5`、`GLOBAL_PCT=95`、`LOCAL_PCT=90`、`LOCAL_CELL=12`、`BAND=1.0`。

---

## v0.10 ｜ 局部与全局都取前10%
- **日期**：2026-08-31
- **主要功能**：把双重密度门槛改为**局部与全局都取前10%**（`GLOBAL_PCT=90`、`LOCAL_PCT=90`）。
- **用户评价/反馈**：用户「都改成局部和全局的前10%」。结果：RESULT 红点 8054(3.9%)、CLEAN 红点 5415(4.9%)，外墙环连续细、误差点被剔除，内部柱橙色。
- **参数**：`COL_GRID=0.3`、`GRID=0.5`、`GLOBAL_PCT=90`、`LOCAL_PCT=90`、`LOCAL_CELL=12`、`BAND=1.0`。

---

## v0.11 ｜ 剔除墙外噪声点（连通域过滤）
- **日期**：2026-09-01
- **主要功能**：解决"实际墙体外的噪声点被误识别成外墙"：
  - footte footprint 栅格做**连通域标记，只保留最大连通块**（`KEEP_ONLY_BIGGEST=True`）——脱离主体的孤立噪声点/块直接被丢弃；
  - **只有落在主 footprint 内的点**才可能是外圈候选；落在被剔除噪声块里的点单独标为"**墙外噪声(已剔除)**"（紫色），不再计入外圈。
  - 竖直密度门槛(全局/局域前10%)照旧滤除稀疏噪声。
- **用法**：`python code/outer.py ../CLEAN_UNDER_GROUND.ply ../RESULT_B1.ply`。
- **用户评价/反馈**：用户提出"墙体外的噪声点无法去除，导致高密度区把噪声误识别成外墙，有什么解决办法"——据此采用最初任务"只保留最大聚合体"的思路。结果：CLEAN 剔除墙外噪声 317 点、RESULT 剔除 5921 点；红环=仅主 footprint 外圈，不含噪声。
- **局限**：只处理"脱离主体的孤立噪声块"；紧贴/连在墙上但稀疏的噪声靠竖直密度门槛过滤。
- **参数**：`KEEP_ONLY_BIGGEST=True`（可关闭）；其余同 v0.10。

---

## v0.12 ｜ 栅格改为 0.1m 提高精度（两尺度 + 结论）
- **日期**：2026-09-01
- **背景（实测）**：两个文件的点间距其实很细——CLEAN 平均 0.11m/中位 0.06m，RESULT 平均 0.023m。原 0.5m 降采样丢了不少细节。
- **改动**：
  - `VOXEL=0.1`（输出/几何降采样）——基础点更精，`*_wall.ply` 更密（CLEAN 44388 点、RESULT 79039 点 vs 原 36474/9750）。
  - 引入**两尺度**：`CONN_VOXEL=0.5` 只用于"去噪/保留最大聚合体"（0.5m 已验证对两文件能保住完整楼体），`VOXEL=0.1` 用于输出/几何。解决"0.1m 下连通/去噪碎裂"（`denoise_and_largest` 增加 `voxel` 参数与闭运算桥接）。
- **关键结论（诚实）**：0.1m 让**基础点云精度真正提高**，但**轮廓栅格**一味调细不会更好——轮廓精度受**外墙连续性**限制：RESULT(0.02m 密) 在 0.2m 栅格下轮廓干净（6 顶点），CLEAN(0.11m 且墙有缝) 在 0.2m 下反而**断口+细刺**（`contour.py` 曾试 0.1/0.2 → CLEAN 面积崩/出毛刺）。故 `contour.RASTER_GRID` 仍用 **0.5m**（最稳无断口），基础精度保留 0.1m。
- **用户评价/反馈**：用户「把栅格大小变为 0.1m×0.1m，精度是否会变高，请实现」——经实测：**点云精度变高（两尺度0.1m）；但轮廓精度受外墙连续性限制，建议轮廓栅格保持0.5m**。
- **当前参数**：`preprocess: VOXEL=0.1, CONN_VOXEL=0.5, MIN_NEIGHBORS=3`；`contour: RASTER_GRID=0.5`；`outer: GRID=0.5, COL_GRID=0.3`。

---

## v0.13 ｜ 高密度点云单元标注（0.1m 格）
- **日期**：2026-09-01
- **主要功能**：**不描轮廓**，把"高密度点云单元"标出来：
  - 沿用之前"统计栅格内点数划分高低密度"的方法，仅把**栅格从 0.3×0.3 改为 0.1×0.1**，重新按每格点数统计。
  - 高密度单元 = **局域内前5% 且 全局前10%** 的格子 → 标成**红色方块**；普通占用格浅蓝、空格白。
  - 输出：`*_高密度单元.png`（高 dpi 以便 0.1m 格可分辨）。**其它步骤（外圈点云等）不变**。
- **用法**：`python code/outer.py ../CLEAN_UNDER_GROUND.ply ../RESULT_B1.ply`。
- **用户评价/反馈**：用户「之前是统计 0.3×0.3 栅格内点数划高低密度的，现在其它步骤不变，只把栅格改成 0.1×0.1 重新统计；高密度=局域前5%且全局前10%的单元标红」。实现：CLEAN 红格 7810、RESULT 红格 5830。
- **参数**：`CELL=0.1`(密度格)、`GLOBAL_PCT=90`(全局前10%)、单元用 `local_pct=95`(局域前5%)、`LOCAL_CELL=12`。

---

## v0.14 ｜ outer.py 按用户版重写 + 支持 LAS，对全部 LAS 运行
- **日期**：2026-09-01
- **主要功能**：`outer.py` 定为**仅标注高密度点云**（点级上色，不勾勒/连接外墙轮廓）：
  - 竖直密度(0.3m 柱) → **全局前10% ∧ 局域前10%**为高密度点；再结合**外圈位置**(距外边界≤`BAND`) → 红=外圈高密度墙点、橙=高密度但不在外圈、浅蓝=其余。
  - 新增**LAS 读取**（自写 LAS 1.x 读取器 + 扩展名分发），现支持 `.las` 与 `.ply`。
  - 输出 `*_外圈点云.png`。
- **用法**：`python code/outer.py ../粟塘B1.las ../雅德B1.las`。
- **运行（清空输出文件夹后）**：对文件夹内所有 LAS（粟塘B1、雅德B1）运行本版，输出仅 `*_外圈点云.png` 两个文件（无 wall.ply/轮廓线等"连外墙"结果，符合要求）。
  - 粟塘B1：红(外圈高密度墙点) 94.4万(1.9%)，橙(内部柱) 254万；全局阈值 2429 点/柱。
  - 雅德B1：红 17.0万(1.2%)，橙 75万；全局阈值 2303 点/柱。
- **用户评价/反馈**：用户确认"这一版代码没有连接外墙的步骤，只有标注高密度点云这一步"，并要求"以该版为基础、对全部 LAS 运行、运行前清空文件夹"。

---

## v0.15 ｜ outer.py 大点云提速 + 新增 4 个 LAS 全跑
- **日期**：2026-09-01
- **主要功能（优化）**：
  - 局域分位（局部前10%）改为**排序分组**计算，避免逐区域 `np.where` 的 O(N×区域) 慢循环——大点云提速数倍。
  - 点染图对"其余点"**抽样**（≤200万点），红/橙全保留，避免 3500 万点散点卡死。
- **运行**：文件夹新增 4 个 LAS（华瑞B1/B2、山水林城B1/B2），全部跑 `outer.py` 高密度标注。结果：
  - 华瑞B1（~3500万点）：红(外圈高密度墙点) 82670(0.2%)，橙 239万；全局阈值 2964 点/柱。
  - 华瑞B2（~3500万点）：红 414204(1.1%)，橙 211万；全局阈值 2425 点/柱。
  - 山水林城B1：红 7900(0.9%)，橙 5.4万；阈值 101 点/柱。
  - 山水林城B2：红 11116(1.5%)，橙 4.3万；阈值 104 点/柱。
  - 至此 6 个 LAS 均输出 `*_外圈点云.png`。
- **用户评价/反馈**：用户"把新增的4个las文件都跑一遍"——已全部跑通并优化大点云性能。

---

## v0.16 ｜ 基本单元改为 0.1m×0.1m
- **日期**：2026-09-01
- **改动**：`COL_GRID` 由 0.3 → **0.1**（竖直密度的 XY 小柱边 = 基本单元 0.1m×0.1m），其余参数不变。判据仍为 竖直密度(0.1m柱) 全局前10% ∧ 局域前10%(12m窗口) ∧ 外圈位置(≤1m)。
- **运行**：全部 8 个数据（6 LAS + 2 PLY）重跑。结果（红=外圈高密度墙点）：
  - CLEAN 4876(4.4%)、RESULT 8691(4.2%)、粟塘B1 108.3万(2.2%)、雅德B1 22.8万(1.6%)、华瑞B1 22.7万(0.6%)、华瑞B2 46.1万(1.2%)、山水林城B1 7800(0.9%)、山水林城B2 11137(1.5%)。
- **用户评价/反馈**：用户问"基本单元多大"，确认 0.3m；随后"把基本单元改成 0.1×0.1"——已改并重跑。

---

## v0.17 ｜ 正交轮廓（方案A：wall.ply 连线 + snap_1d 直角化）
- **日期**：2026-09-01
- **背景**：用户提供一版 GUI 轮廓代码（含 `snap_1d_coordinates` 一维聚类拉平 / `remove_collinear_points` 去共线 / buffer 融合搭桥），让我借鉴"连线"手法，把外圈高密度点连成轮廓。
- **诊断**：v0.16 的"红点(外圈高密度)"本身是不连续结构点（墙+柱混合、有断口），直接连线会碎/成梳齿（曾得 7~1984 m²）。可靠来源是 **preprocess 的整面墙点 wall.ply**。
- **主要功能（方案A，用户选定）**：
  - `outline.py` 读 `*_wall.ply` → `contour.trace_outline` 连成闭合轮廓 → **在建筑主方向(min外接矩形)坐标系内 `snap_1d` 直角拉平** → `remove_collinear_points` 去共线 → 输出 `*_正交轮廓.png/.json`。
  - 在主方向内正交化，故旋转类车库(雅德)也能得到带直角的规整矩形。
  - 借鉴用户的 `snap_1d_coordinates`、`remove_collinear_points`；`ORTHO_TOL=3.0`(聚类容差)。
- **用法**：`python code/outline.py 输出/粟塘B1_wall.ply 输出/雅德B1_wall.ply`。
- **用户评价/反馈**：用户选择方案A（wall.ply 连线 + snap_1d 直角化）。结果：粟塘B1 71 顶点/9604 m²/871m（贴合footprint、横平竖直含缺口与延伸）；雅德B1 24 顶点/2515 m²/265m（带直角的规整旋转矩形）。红色高密度标注(outer.py)仅作可视化，不参与连线。

---

## v0.18 ｜ outline.py 增加轮廓简化容差（更正交干净）
- **日期**：2026-09-01
- **背景 / 反馈**：用户看 v0.17 结果，反馈"RESULT稍好，另一个(CLEAN/粟塘)很糟糕"——因为 `snap_1d`(容差3m)压不掉 3m 以上的微台阶，CLEAN 留 71 顶点、全是小折线。
- **改动**：连线后、正交化前，先做一次 **Douglas-Peucker 简化**（`SIMPLIFY_TOL`，默认 6.0m），把微台阶并成直段再 `snap_1d` 直角化 → 更干净、顶点更少。
- **结果（SIMPLIFY_TOL=6.0）**：CLEAN 23 顶点/9479 m²/787m；RESULT 5 顶点/2224 m²/202m；粟塘 23 顶点/9749 m²；雅德 9 顶点/2399 m²。RESULT 底部小缺口被简化掉。
- **权衡**：`SIMPLIFY_TOL` 越大越干净/顶点越少，但会磨掉小细节（如 RESULT 缺口）；越小越贴细节但台阶多。
- **用法**：`python code/outline.py 输出/*_wall.ply`（参数在 `outline.py` 顶部：`SIMPLIFY_TOL`、`ORTHO_TOL`）。

---

## v0.19 ｜ 采用用户版"连线规则"（线必须水平/竖直）
- **日期**：2026-09-01
- **改动**：`outline.py` 增加 `orthogonal_connect`，**完全按用户代码的连线规则**：网格化 → `MultiPoint.buffer(格宽/2*1.05)` → `buffer(桥距).buffer(-桥距)` 搭桥 → `snap_1d_coordinates` 一维聚类强拉平（线只剩水平/竖直）→ `Polygon.buffer(0)` 修复 → `remove_collinear_points` 去共线。参数 `GRID_SIZE=0.3`、`BRIDGE_DIST=1.5`、`FLATTEN_TOL=3.0`。
- **关键诊断（反馈给用户）**：该连线规则要求输入**空间连续/可闭合**。直接把 **红点(外墙高密度)或红+橙**喂进去会**碎掉/退化**（面积 0~15 m²，因为高密度点是结构元件，彼此有空隙 >1.5m，buffer 桥接不上）。**wall.ply（preprocess 输出的整面墙点，连续）** + 该规则 → 成功闭合且线全水平/竖直：粟塘 79 顶点/9719 m²/883m、雅德 33 顶点/2634 m²/294m。
- **注意**：用户代码里的形态学滤毛刺 `buffer(-1.0).buffer(1.0)` 会把**细墙环削没**（红点/墙环厚度<2m），设 `MORPH=0` 关闭。
- **用户评价/反馈**：用户提供连线规则代码，要求"提取外墙高密度点后按其规则连接，线必须水平或竖直"——已实现；并反馈"规则需要连续输入"这一工程事实。

---

## v0.20 ｜ 自适应朝向正交（水平的水平、倾斜的倾斜且线线90°）
- **日期**：2026-09-01
- **背景 / 反馈**：用户看 v0.19 结果：粟塘(轴对齐)OK；雅德(整体倾斜)线虽直却非正交、且被强行掰平。要求"**水平的就输出水平、倾斜的就输出倾斜，且线与线尽可能正交**"。
- **改动**：`orthogonal_connect` 增加**朝向自适应**：先算融合多边形的主方向角 `ang`（最小外接矩形，归一化到[-45°,45°]）；
  - `|ang| ≤ ORIENT_THRESH(8°)` → 视作轴对齐建筑，在**世界系** `snap_1d` → 线严格水平/竖直（粟塘）；
  - `|ang| > 阈值` → 在**主方向坐标系**内 `snap_1d` 再转回 → 线随建筑倾斜但**相邻线 90° 正交**（雅德）。
- **结果**：粟塘 79 顶点/9717 m²（全水平竖直）；雅德 37 顶点/2610 m²（斜着但线线正交）。
- **用法**：`python code/outline.py 输出/*_wall.ply`（参数：`GRID_SIZE=0.3`、`BRIDGE_DIST=1.5`、`FLATTEN_TOL=3.0`、`ORIENT_THRESH=8.0`）。

---

## v0.21 ｜ FLATTEN_TOL 3→5（并干净底部台阶）
- **日期**：2026-09-01
- **背景 / 反馈**：用户对两个 PLY 跑 v0.20 后指出 "RESULT_B1 底部有一排小台阶，需要并干净"——这是 `snap_1d` 容差(3m)不足，台阶 >3m 时保留。
- **改动**：`FLATTEN_TOL` 由 3.0 → **5.0**。结果：
  - RESULT_B1：42 → **9 顶点**/2292 m²/208m，底部台阶并成一条条斜线（斜线间仍 90° 正交）。
  - CLEAN_UNDER_GROUND：59 顶点/10069 m²，仍全水平/竖直、贴合 footprint。
  - 对应 LAS 版（粟塘/雅德）未重跑（用户暂不需要对雅德操作）。
- **权衡**：`FLATTEN_TOL` 越大越干净（台阶并没了），但会把 <容差 的真实小角也并掉。

---

## v0.22 ｜ 尝试在正交轮廓中保留"斜线 + 圆弧"（已实现；当前数据未触发）
- **日期**：2026-09-02
- **背景**：用户指出"规则水平竖直结构"除直线外还有**圆弧**（CLEAN 左上角）与**小斜线**（右上角），希望把这 2 类加入代码。用户确认尺度：**圆弧 R≥5m 且跨度≥25°；斜线 长≥2m 且偏离≥2°**。
- **实现（`code/outline.py`）**：
  - 分段/拟合工具箱：`_turns`(转弯率平滑)、`segmentize`、`fit_line`、`fit_circle`(Kasa)、`_line_circle_pt`；
  - 增强 `enhance_arcs_slants`（角上圆角 fillet：基底角附近原始墙边拟合圆 → 切点+采样替换角；兼保留斜线弦）；
  - 管线：基底(稳定 snap_1d 正交) → 增强 --失败/校验不过则**原样保留基底**。
- **诚实结论（两 PLY 实测）**：CLEAN 59 顶点/10069 m²、RESULT 9 顶点/2292 m²（= v0.21 干净正交底图，增强未触发）。原因：融合边界近角处锯齿大（平滑后转弯率均值 10~30°/点），**角附近短弧段圆拟合残差>1.2m 仍超阈、半径不稳** → 圆弧/斜线校验不通过，自动 no-op。
- **参数**：`ARC_R_MIN=5, ARC_R_MAX=200, ARC_MIN_SPAN=25, ARC_RESIDUAL_MAX=0.8, SLANT_TOL=2, SLANT_MIN_LEN=2, STAIR_LEN=4, PATCH_NEAR=2.5`。
- **待优化**：需要更平滑的边界源（更细网格/样条平滑）或放宽校验，圆弧才会可靠触发；当前结果稳定、不崩，可先用。

---

## v0.22（修订）｜ 圆弧保留真正生效（CLEAN 左上圆弧出现）
- **日期**：2026-09-02
- **关键修复与调试结论**：
  1. 取点半径 2.5m 只覆盖弧顶 → 拟合 R=1~2m 局部小圆；**PATCH_NEAR=8.0**（约等于真实弧 R）后，融合边界拟合稳定：左上角 R=9.8/残差0.24 ✅，普通直角 R=1.5 ❌ —— 能区分真弧！
  2. 拟合源用**融合边界（纯轮廓、无内部柱污染）**，不用全部墙点；用 `fit_circle_trimmed`（迭代剔除残差点、抗直墙污染）。
  3. **相切判别式截断**：墙线是圆弧切线，判别式浮点可为负 → `max(disc,0)` 取切点（此前直接 None 导致一直回退）。
  4. 圆角条件收紧：转角40°~140°、**两邻边均≥6m**、切点落在边内、**仅取短弧(25°~170°)** → 避免多角重叠成大圆圈/尖刺。
- **结果**：CLEAN 82 顶点/10058 m²/918m（**左上角出现保留圆弧**，其余角保持正交）；RESULT 9 顶点/2292 m²（无回归）。对比 v0.21 基图，已显著不同——arc 圆角生效。
- **诚实说明**：右上"小斜线"目前仍走"非弧保留角"路径（斜线弦逻辑在代码内但未在 CLEAN 触发可见变化）；下一步可单独做"短斜线段保留"。
- **参数**：`PATCH_NEAR=8.0`、`fit_circle_trimmed`、`l1/l2>=6`、`ARC_MIN_SPAN=25` 等见 `outline.py`。
