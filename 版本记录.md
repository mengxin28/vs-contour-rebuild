# 版本记录（每一版的 主要功能 / 用法 / 用户评价）

> 约定：本仓库从「0.3m 热力密度图」这一版开始记录版本。每新增一版提交，就追加一条。
> 「用户评价」记录用户对该版的反馈、要求或修改意见（据此决定后续开发方向）。

---

## v0.1 ｜ 首个入库版本：0.3m 热力密度图版（当前）
- **日期**：2026-08-30
- **主要功能**：
  - 读取地下车库点云（`.las` 与 `.ply`，兼容带/不带 RGB）。
  - `0.5m` 体素降采样 → 去孤立噪点 → 只保留最大连通聚合体 → 法向量判别去地面/天花板 → 投影到水平面（Z 归零）。
  - 输出：外墙点云 `_wall.ply`、`_俯视_原始/外墙.png`、`_透视_外墙.png`、`汇总.json`。
  - **新增 `0.3m × 0.3m` 热力密度图** `_热力密度图.png`：用**原始分辨率点**（仅限墙体结构内）统计密度，
    采用对数色标（`LogNorm`）渲染，突出「外墙密集连续」的墙带。
- **用法**：
  ```bash
  python code/preprocess.py CLEAN_UNDER_GROUND.ply RESULT_B1.ply
  ```
- **用户评价/反馈（本期汇总）**：
  - 最初要求仅处理 LAS，必须做 0.5m 降采样，且要有可视化图片供查看。
  - 之后改为换两个 PLY（CLEAN_UNDER_GROUND、RESULT_B1）运行，并删除之前 LAS 的结果。
  - 下一步要求做 **0.3m 热力密度图** —— 本版已实现。
  - 同时要求建立 GitHub 仓库、把每一版代码入库并标注（功能 / 用法 / 评价），且 `.gitignore` 不上传。
  - 处理结果：粟塘体约 256×93 m、雅德体约 75.7×54.7 m，外墙墙带在密度图上明显高亮。

---

## v0.2 ｜ 规则外轮廓提取版（`code/contour.py`）
- **日期**：2026-08-30
- **主要功能**：
  - 从阶段一输出的 `*_wall.ply`（投影后 Z=0 的外墙点云）提取**规则外轮廓多边形**。
  - 方法：凹包（`shapely.concave_hull`，`CONCAVE_RATIO=0.15`）→ 取最大多边形 → Douglas-Peucker 简化（`SIMPLIFY_TOL=0.8`，可并存）成直线段多边形。
  - 同时输出**最小外接旋转矩形**作为参考（绿色虚线，比真实轮廓更粗放）。
  - 输出：`*_外轮廓.png`（墙点 + 外轮廓 + 外接框）、`*_外轮廓.json`（顶点坐标、面积、周长、外接框）、`外轮廓_汇总.json`。
  - 参数在 `code/contour.py` 顶部可调：`CONCAVE_RATIO`（越小越贴凹边/1=凸包）、`SIMPLIFY_TOL`（简化容差）。调小可更贴合真实墙角但顶点更多。
- **用法**：
  ```bash
  python code/contour.py 输出/CLEAN_UNDER_GROUND_wall.ply 输出/RESULT_B1_wall.ply
  ```
- **用户评价/反馈（本期）**：
  - 要求：进行下一步优化（规则外轮廓提取），上传代码外还需告知**输出结果位置**，用户会据此让步骤调试调整。
  - 效果：CLEAN 31 顶点（面积 12142 m²/周长 681 m，贴合右下延伸块与顶部缺口）；RESULT 10 顶点（面积 2449 m²，贴合旋转矩形底部缺口）。
  - 待优化方向（用户可能随后要求）：顶点偏多/偏毛糙、是否需要把边吸附到主轴、是否只保留外墙壳（去掉内部柱）、简化容差/凹包比率的取舍。

---

## v0.3 ｜ 贴合外墙轮廓线版（`code/contour.py`，方法重写）
- **日期**：2026-08-30
- **主要功能**：改为输出**贴合外沿墙体走向的轮廓线**，而不是用凹包把区域包成闭合多边形。
  - 方法：墙点栅格化(0.5m) → 闭运算连缝 → `binary_fill_holes` 填内部空腔 → `binary_opening` 去一格外伸细刺 → 提取外沿轮廓（marching squares）→ shapely 简化。
  - 差异：这条线**沿真实墙体外沿走线**，凹口/空腔处不斜穿、不包住（比凹包面积更小、周长更长、顶点更多）。
  - 参数：`RASTER_GRID=0.5`(栅格)、`CLOSE_ITER=2`(连缝)、`OPEN_KERNEL=2`(去刺)、`SIMPLIFY_TOL=0.8`。输出 `*_外轮廓.png`、`*_外轮廓.json`、`外轮廓_汇总.json`。
- **用法**：同 v0.2（`python code/contour.py 输出/*_wall.ply`）。
- **用户评价/反馈**：用户明确指出「不要凹包那种把区域包起来的轮廓，要一条**贴合外部轮廓的线**」——据此将方法从 concave hull 改为栅格+外沿提取。效果：CLEAN 108 顶点（面积 9630 m²/周长 905 m）、RESULT 28 顶点（面积 2095 m²/周长 252 m），均贴合真实墙沿与转角。
- **待优化方向**（用户可能随后要求）：贴得够但仍有 0.5m 栅格阶梯锯齿；是否把 `SIMPLIFY_TOL` 调大更平滑、或把长边吸附到主轴成更"规则"折线；右侧凹口是否要保留（可能为扫描缺失）。

---

## v0.4 ｜ 规则化外轮廓版（`code/contour.py` 增加规则化）
- **日期**：2026-08-30
- **主要功能**：在 v0.3 贴合线基础上新增**规则化**，输出"工程图样"式规整多边形：
  - 取主方向（最小外接矩形）→ 旋转转正 → Douglas-Peucker 简化（`REG_TOL`，把阶梯去成一条直边，含统一斜角，如雅德的旋转矩形）→ 转回原方向。
  - 参数：`REG_TOL=4.0`（越大越规则、顶点越少）。输出同名 `*_外轮廓.png` / `.json`。
- **用法**：同 v0.2/v0.3（`python code/contour.py 输出/*_wall.ply`）。
- **用户评价/反馈**：用户指出「大体贴合，但真正工程图样不长这样，要做成规则的形状」——据此加规则化。
  - 效果：CLEAN 35 顶点（9747 m²/817 m；v0.3 为 108 顶点）、RESULT 13 顶点（2132 m²/240 m），RESULT 已是干净的旋转矩形。
- **待优化方向**：若还想更平滑、或去掉某处凹口（如 CLEAN 底部 V 形连接凹口）、或吸附到严格横平竖直，可再调 `REG_TOL`，或我再加"方向吸附/直交化"。

---

## v0.5 ｜ 修复轮廓坐标偏移 + 加强可视化（bugfix）
- **日期**：2026-08-30
- **主要功能 / 修复**：
  - **修复坐标整体平移**：v0.4 的"绕原点转正再转回"因车库坐标离原点几百米、旋转角较大，使多边形整体偏移几百米，导致轮廓跑到点云视野之外、用户"看不到"。已改为直接 Douglas-Peucker（与方向无关），坐标归位。
  - 可视化增强：轮廓画成**粗红实线 + 顶点圆点**；新增**纯轮廓线图 `*_轮廓线.png`**（白底、红粗线、含顶点），便于一眼查看。
- **用法**：同前（`python code/contour.py 输出/*_wall.ply`）。
- **用户评价/反馈**：用户「轮廓是什么颜色，我看不到你提取出来的轮廓」——排查发现是坐标被旋转平移所致，修复后轮廓以**红色实线**清晰可见。RESULT 13 顶点、CLEAN 35 顶点。

---

## v0.6 ｜ 外圈高密度点云高亮版（`code/outer.py` 新增）
- **日期**：2026-08-31
- **主要功能**：**不描轮廓线**，把**外侧高密度点云（外墙）用红色**高亮，其余点浅色。
  - 方法：墙点栅格化(0.5m) → 闭运算连缝 → 填内部空腔 → `distance_transform_edt` 求"到外边界距离" → 距离 ≤ `BAND` 判为外圈点 → 标红。
  - 输出：`*_外圈点云.png`（红=外墙外圈，蓝=内部/其余点）。参数 `GRID=0.5`、`CLOSE_ITER=2`、`BAND=2.0`(m)。
- **用法**：`python code/outer.py 输出/*_wall.ply`。
- **用户评价/反馈**：用户「先不要描外轮廓，先把外面高密度的点云用红色表示出来」——据此新增。效果：外墙在图中成明显红色外环，内柱浅色。外圈点占比：CLEAN 43%(15676/36474)、RESULT 52%(5041/9753)。
- **待调**：`BAND` 控制外圈厚度（如需更细/更粗的外圈环即调它）。
